<div class="body">

  <table border="0" cellpadding="0" cellspacing="0">
    <tbody>
      <tr>
        <td>
          <h2>{{title}}</h2>
          <p>
            {{text}}
          </p>
          <a href="{{email.button.link}}" target="_blank" title="{{email.button.text}}">{{button.text}}</a>
          <p>
            {{button.link}}
          </p>
        </td>
      </tr>
    </tbody>
  </table>
</div>
