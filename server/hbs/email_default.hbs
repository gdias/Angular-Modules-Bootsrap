<div class="body">
  <table border="0" cellpadding="0" cellspacing="0">
    <tbody>
      <tr>
        <td>
          <h2>{{title}}</h2>
          <p>
            {{{text}}}
          </p>
          <a href="{{button.link}}" target="_blank" title="{{button.text}}">{{button.text}}</a>
        </td>
      </tr>
    </tbody>
  </table>
</div>
